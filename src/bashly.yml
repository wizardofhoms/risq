name: risq 
help: RISK AppVM client 
version: 0.1.0

dependencies:
  - qvm-pass

commands:
  - name: pass 
    help: "Use the password-store in vault"
    catch_all: true
    args:
      - name: command
        help: "Pass command to use"
